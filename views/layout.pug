doctype html
html
  head
    //Google Analytics
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-112717734-1"></script>
    script.
      window.dataLayer = window.dataLayer || [];
      function gtag() {
          dataLayer.push(arguments);
      }
      gtag('js', new Date());
      gtag('config', 'UA-112717734-1');
    title= title
    link(rel="stylesheet", href="/stylesheets/all.css")
    block scripts
  body
    .wrapper
      block navbar
      block content
      .push
    .footer
      .alpha-warning
        span
          i.glyphicon.glyphicon-warning-sign(style="margin-top:10px;font-size: 30px; color: #ffd900;")
          h3(style="color: #333333; margin-top: -10px;")
          | This is still in development(alpha stage), the finished site will be a lot different from current one. There can be errors.
      p.author(style="text-align: center;") Coded by Daugardas
